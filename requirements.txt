# ═══════════════════════════════════════════════════
#  Biogas Platform — CPU-Only Requirements
#  Railway Free-Tier Compatible (No GPU / No CUDA)
# ═══════════════════════════════════════════════════

# ─── Core Framework ───────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-multipart>=0.0.9

# ─── CPU-Only PyTorch ─────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# ─── YOLO (CPU inference only) ────────────────────
ultralytics>=8.0.0

# ─── ML Models (CPU only) ────────────────────────
scikit-learn>=1.3.0
xgboost>=2.0.0
numpy>=1.24.0
pandas>=2.0.0

# ─── Image Processing (headless — no GUI deps) ───
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# ─── Optimization ────────────────────────────────
optuna>=3.0.0

# ─── Explainability ──────────────────────────────
shap>=0.44.0

# ─── Database (PostgreSQL on Railway) ─────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.20.0
